{% extends 'base.html.twig' %}

{% block title %}Reference{% endblock %}

{% block body %}
    <h1>Reference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reference.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ reference.title }}</td>
            </tr>
            <tr>
                <th>DisplayedDate</th>
                <td>{{ reference.displayedDate }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ reference.startDate ? reference.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ reference.endDate ? reference.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ reference.createdAt ? reference.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reference_index') }}">back to list</a>

    <a href="{{ path('reference_edit', {'id': reference.id}) }}">edit</a>

    {{ include('reference/_delete_form.html.twig') }}
{% endblock %}